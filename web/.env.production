# Not a secret — just the public API origin for production builds.
# Update this if the Worker is ever redeployed under a different name/domain
# (e.g. once totaltossup.live is wired up as a custom domain).
VITE_API_ORIGIN=https://total-tossup-live.total-tossup-live-worker.workers.dev
